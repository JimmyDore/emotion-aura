// Entry point -- skeleton for Task 1.
// Task 2 will add SceneManager, Stats, and the render loop.
import './style.css';

document.addEventListener('DOMContentLoaded', () => {
  console.log('Emotion Aura: scaffold ready');
});
